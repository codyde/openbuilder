# Runner Configuration
# Copy this file to .env.local and update values
# Note: These are only used when running the runner directly (not via CLI)

# AI API Keys (Required for builds)
ANTHROPIC_API_KEY=your-anthropic-api-key-here
OPENAI_API_KEY=your-openai-api-key-here

# SDK Feature Flags (Optional - enable additional SDK options)
# By default, only Agent SDK (Claude + Codex models) is available.
#
# Enable OpenCode SDK (multi-provider support):
# ENABLE_OPENCODE_SDK=true
# OPENCODE_URL=http://localhost:4096   # Required when ENABLE_OPENCODE_SDK=true
#
# Enable Factory Droid SDK:
# ENABLE_FACTORY_SDK=true

# Shared secret for authenticating with broker
RUNNER_SHARED_SECRET=dev-secret

# Broker WebSocket URL
RUNNER_BROKER_URL=ws://localhost:4000/socket

# API base URL
API_BASE_URL=http://localhost:3000

# Runner identifier
RUNNER_ID=local

# Workspace directory for generated projects
WORKSPACE_ROOT=/Users/yourname/hatchway-workspace

# Sentry DSN for error tracking
SENTRY_DSN=your-sentry-dsn-here
